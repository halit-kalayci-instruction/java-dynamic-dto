spring:
  application:
    name: dtoapp
  datasource:
    url: jdbc:postgresql://localhost:5432/dto
    username: postgres
    password: test
  jpa:
    hibernate:
      ddl-auto: update
  redis:
    host: localhost
    port: 6379
server:
  port: 8085